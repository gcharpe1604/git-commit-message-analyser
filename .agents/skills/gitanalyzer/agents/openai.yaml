interface:
  display_name: "Gitanalyzer"
  short_description: "Repo-specific patterns and workflows for gitanalyzer"
  default_prompt: "Use the gitanalyzer repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true